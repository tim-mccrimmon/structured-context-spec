id: bundle:{{ project_name }}
type: project
version: "DRAFT"
title: "{{ project_name }} Complete Context Bundle"
description: "Complete structured context for {{ project_name }}"

imports:
  # Foundation bundles (unversioned - the imported bundles are DRAFT too,
  # pin a real version once you've run `scs bundle version` on each)
  - bundle:meta
  - bundle:standards

  # Domain bundles
  {% for bundle in bundles -%}
  - bundle:{{ bundle }}
  {% endfor %}

scds: []  # All SCDs are in imported bundles

provenance:
  created_by: "{{ email }}"
  created_at: "{{ created_at }}"
  rationale: "Initial project bundle for {{ project_name }} development"
