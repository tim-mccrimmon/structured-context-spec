id: scd:project:policy-enforcement
type: project
version: "1.0.0"
status: DRAFT
title: "Policy Enforcement"
description: "Mechanisms for enforcing compliance and security policies"

domain: compliance-governance
concerns:
  - policy-enforcement
  - automated-controls

content:
  automated_enforcement:
    - policy: "[Policy name]"
      mechanism: "[Enforcement mechanism]"
      enforcement: "[How it's enforced]"

  access_control_enforcement:
    - policy: "[Access policy]"
      mechanism: "[Control mechanism]"
      enforcement: "[Enforcement method]"

  manual_controls:
    - policy: "[Manual policy]"
      mechanism: "[Review process]"
      enforcement: "[Sign-off requirements]"

relationships:
  # Define how this SCD relates to others in your project.
  # Relationship types: depends-on, satisfies, constrains, refines, extends, conflicts-with, implements
  #
  # Example:
  # - type: "depends-on"
  #   target: "scd:project:other-scd-name"
  #   description: "Why this relationship exists"
  # - type: "satisfies"
  #   target: "scd:standards:compliance-requirement"
  #   description: "How this SCD satisfies the requirement"

provenance:
  created_by: "{{ email }}"
  created_at: "{{ created_at }}"
  rationale: "Initial policy enforcement for {{ project_name }}"
