id: scd:project:tech-stack
type: project
version: "1.0.0"
status: DRAFT
title: "Technology Stack"
description: "Programming languages, frameworks, databases, and infrastructure choices"

domain: architecture
concerns:
  - technology-choices
  - dependencies

content:
  languages:
    primary:
      - name: "[Language name]"
        version: "[Version]"
        rationale: "[Why this language]"
    secondary:
      - name: "[Language name]"
        version: "[Version]"
        use_case: "[Specific use case]"

  frameworks:
    backend:
      - name: "[Framework name]"
        version: "[Version]"
        rationale: "[Why this framework]"
    frontend:
      - name: "[Framework name]"
        version: "[Version]"
        rationale: "[Why this framework]"

  databases:
    - name: "[Database name]"
      type: "[SQL|NoSQL|Graph|etc]"
      version: "[Version]"
      use_case: "[Primary use case]"
      rationale: "[Why this database]"

  infrastructure:
    cloud_provider: "[AWS|Azure|GCP|None]"
    container_runtime: "[Docker|Podman|None]"
    orchestration: "[Kubernetes|ECS|None]"
    rationale: "[Infrastructure choices rationale]"

  third_party_services:
    - name: "[Service name]"
      purpose: "[What it's used for]"
      vendor: "[Vendor name]"
      pricing_model: "[Free|Usage-based|Subscription]"

  development_tools:
    version_control: "Git"
    ci_cd: "[GitHub Actions|GitLab CI|Jenkins|etc]"
    monitoring: "[Tool name]"
    logging: "[Tool name]"

relationships:
  # Define how this SCD relates to others in your project.
  # Relationship types: depends-on, satisfies, constrains, refines, extends, conflicts-with, implements
  #
  # Tech Stack is foundational - component and infrastructure design depend on it.
  # Example relationships:
  - type: "depends-on"
    target: "scd:project:business-objectives"
    description: "Technology choices align with business goals"
  # - type: "constrains"
  #   target: "scd:project:component-model"
  #   description: "Technology choices constrain component implementation"
  # - type: "constrains"
  #   target: "scd:project:infrastructure-definition"
  #   description: "Technology choices constrain infrastructure options"

provenance:
  created_by: "{{ email }}"
  created_at: "{{ created_at }}"
  rationale: "Technology stack definition for {{ project_name }}"
