id: scd:project:data-protection
type: project
version: "0.1.0"
domain: security
title: "Data Protection"
description: "Data protection and encryption requirements"

content:
  summary: "Encryption at rest and in transit for all PHI"

  encryption_at_rest:
    database: "AES-256 via AWS RDS encryption"
    storage: "AES-256 via S3 server-side encryption"
    backups: "Encrypted with same key management"
    key_management: "AWS KMS with automatic rotation"

  encryption_in_transit:
    external: "TLS 1.3 required"
    internal: "TLS 1.2+ within VPC"
    api_gateway: "HTTPS only, HSTS enabled"

  sensitive_data_handling:
    phi_fields:
      - "patient_name"
      - "phone_number"
      - "email"
      - "medication_list"
      - "adherence_history"
    masking: "Log masking for PHI fields"
    tokenization: "Patient IDs tokenized in analytics"

  key_rotation:
    frequency: "Annual for data keys"
    procedure: "Zero-downtime rotation via KMS"

provenance:
  created_by: "security-team@medtech.com"
  created_at: "2025-12-01T00:00:00Z"
  rationale: "Encryption requirements from HIPAA Security Rule"
